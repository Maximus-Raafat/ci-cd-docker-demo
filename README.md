# ci-cd-docker-demo
This project demonstrates a basic CI/CD pipeline using Jenkins (Freestyle job) integrated with Docker. When code is pushed to GitHub, Jenkins pulls the repo, builds a Docker image, and runs a container to validate the application, showcasing core DevOps automation concepts.
